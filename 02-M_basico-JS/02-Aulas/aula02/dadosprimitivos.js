//Dados Primitivos String, Number, Undefined, Null, Boolean, Symbol
const nome = 'Clairton'//String
const num = 10 //Number
const num2 = 11.1 // number
//não posso inicializar uma constatnte sem valor nela. tem que ter valor
let  nomeAluno; //undefined = nao aponta para lugar nenhum na memoria
let sobreNomeAluno = null //Nulo ->nao aponta para lugar nenhum na memoria
//null é diferente de Undefined
// null eu posso setar
//undefined é a liguagem que determina, nao posso setar
//primitivos boolean so é true e false(lógico)
const aprovado = true;
const reprovado = false